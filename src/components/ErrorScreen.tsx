import { View, Text } from "react-native";

export default function ErrorScreen() {
  return (
    <View className="flex flex-1 items-center justify-center">
      <Text className="text-2xl text-semibold">An Error Occured</Text>
    </View>
  );
}
